# 造物台失败任务修复与实测

任务 `AUTO-20260831-161428-4118F92A`（打印一只4厘米的小老鼠）已通过正式网站流程恢复，状态为 `ready_to_print`。最终一次恢复用时 342.54 秒，只复用了已有模型，没有重新调用 M1 或模型生成，没有上传或开打。

## 根因与通用修复

- 局部补料使原有大平面重新划分三角形，外形测量错误耗尽容量。先证明整块共面表面的覆盖关系，再以原精度检查未被证明的表面，保持所有原验收阈值。
- 网格边界会误报部分实际已有承托的挤出段。用原距离限制下的整段连续路径覆盖复核，仍拒绝微小真悬空、跨空中段与无可达下层的支撑。
- 稀疏贴合支撑会出现断层和孤立线段。两轮修复预算内增加一次网格布局候选，将主体间距收至 2 mm，保持线宽、接触距离、界面和可拆性检查，不增加永久支柱。
- Auto Orient 输出路径为 241 字符，追加整理后缀后达 271 字符，超过 Windows 默认限制。使用同目录短名独立临时文件并原子替换；失败保护原文件，不删除同名旧临时文件。
- 文件读写、CLI 和测量未完成与几何不合格分别处理，软件错误不再触发无意义的几何重新生成。
- 旧版失败任务可按修复版本复用 M1/M2，仅重新执行本地制造步骤。旧状态保留在任务 `attempt_history/`，每次使用新输出目录；同版本不无限恢复，已经或可能上传/开打的任务不适用。界面单独显示本次修复耗时。

## 最终实测

- 高度 40.000 mm，封闭网格，单连通实体。
- 一轮局部修复增加 1.51514 mm³（约 0.01098%）；模型外廓尺寸与平底不变，源文件哈希不变。
- 真实 Bambu Auto Orient 一次成功，姿态检查通过，倾角 0°。
- Auto Orient 前后真实切片检查均通过，最终危险层数 0。
- 可拆支撑设计检查通过，最小接触间隙 0.24 mm。
- 网站 01 工程、02 切片、03 STL 三个下载均 HTTP 200，内容哈希与最终验收记录逐一相同。
- 140 项相关回归检查通过。真实悬空、外形破坏、粘连支撑、错误姿态和不明打印结果仍拒绝放行。

最终证据：`outputs/printability_recovery_20260831/final_delivery_verification.json`。

真实文件预览：`outputs/printability_recovery_20260831/final_model_and_supports.png`。

正式准备目录：`outputs/automatic_jobs/AUTO-20260831-161428-4118F92A/manufacturing/recheck_44542856c6b7/verified_preparation_t98836s4/`。

回归记录：`outputs/printability_recovery_20260831/regressions.json`。

本轮修改前备份：`历史版本/20260831_printability_recovery/`。

软件验收不等于实物试打。本次未进行实物打印、材料粘结或拆卸力度验证，也不承诺任意形状都能自动修复；无法满足安全条件的输入仍会被拒绝。
